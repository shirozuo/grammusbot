from django.apps import AppConfig


class TgCoreConfig(AppConfig):
    """App configuration for the tg_core application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "tg_core"
